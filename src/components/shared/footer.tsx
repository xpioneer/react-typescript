import * as React from 'react'
import { Layout } from 'antd';

const { Footer } = Layout

export default () => <Footer
  style={{
    background: '#fff',
    textAlign: 'center'}}>
  Copyright by Keefe
</Footer>